PROJECT(tc_indirect_locomotion)
cmake_minimum_required(VERSION 2.6)

SET (CMAKE_MODULE_PATH "${CMAKE_SOURCE_DIR}/.orogen/config")
INCLUDE(tc_indirect_locomotionBase)

# FIND_PACKAGE(KDL)
# FIND_PACKAGE(OCL)

